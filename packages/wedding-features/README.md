# `@entrelacos/wedding-features`

Shared guest-facing presentation boundaries. The current `FeatureAvailability` component is an honest scaffold status surface with a Motion island and reduced-motion support.

It does not implement RSVP, authentication, guest sessions, API calls, or success simulations. Those behaviors require later contracts and vertical slices.
