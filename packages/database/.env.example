# Server-side migration connection only. Choose the environment explicitly.
# Never use main for tests. Never place this value in PUBLIC_* or VITE_* variables.
DATABASE_URL=
DATABASE_URL_TEST=
# Development/test identity guards only. Production derives its checks from DATABASE_URL.
DATABASE_PROJECT_ID=
DATABASE_DEVELOPMENT_BRANCH_ID=
DATABASE_TEST_BRANCH_ID=
DATABASE_NAME=
