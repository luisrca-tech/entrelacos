# Template boundary fixture

Private technical consumer for `@entrelacos/template-root/v1`. This fixture intentionally has no guest API, database, SMS, or product polish.

It proves that a host can provide distinct serializable content, metadata, navigation, media, a reordered home composition, a host-owned local section, and an additional `/hospedagem` route without copying template source or importing a private path.
